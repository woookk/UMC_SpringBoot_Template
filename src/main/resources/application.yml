server:
  port: 9000  # 9000번 포트로 실행

spring:
  application:
    name: demo

  datasource:
    platform: mysql
    url: jdbc:mysql://umc-server-rds-test.c1obql0qiytf.ap-northeast-2.rds.amazonaws.com/UMC_Server
    # url 형식은 jdbc:mysql://RDS의 EndPoint/내부 DB 스키마명 입니다.
    username: root
    password: dnrdl4630
    driver-class-name: com.mysql.cj.jdbc.Driver
